{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dispatch.arch/schemas/events/produced/order/order.rto_delivered.json",
  "title": "Order RTO Delivered Event",
  "description": "Rider-facing event published when RTO package is returned to origin",
  "type": "object",
  "required": [
    "event_type",
    "event_version",
    "event_id",
    "dispatch_order_id",
    "rider_id",
    "client_order_id",
    "traceparent",
    "timestamp"
  ],
  "properties": {
    "event_type": {
      "type": "string",
      "const": "order.rto_delivered",
      "description": "Event type discriminator"
    },
    "event_version": {
      "type": "integer",
      "minimum": 1,
      "description": "Event schema version for evolution"
    },
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique event identifier for deduplication"
    },
    "dispatch_order_id": {
      "type": "string",
      "description": "Order identifier in human-readable format"
    },
    "rider_id": {
      "type": "string",
      "description": "Assigned rider identifier"
    },
    "client_order_id": {
      "type": "string",
      "description": "Client-facing order identifier"
    },
    "quote_id": {
      "type": "string",
      "format": "uuid",
      "description": "Associated quote ID"
    },
    "rto_delivery_location": {
      "type": "object",
      "properties": {
        "lat": {
          "type": "number",
          "description": "RTO delivery latitude"
        },
        "lng": {
          "type": "number",
          "description": "RTO delivery longitude"
        },
        "address": {
          "type": "string",
          "description": "RTO delivery address"
        }
      },
      "description": "Location where RTO package was delivered"
    },
    "recipient_name": {
      "type": "string",
      "description": "Name of person who received the RTO package"
    },
    "recipient_relationship": {
      "type": "string",
      "description": "Relationship of recipient to sender (e.g., 'self', 'family member', 'staff')"
    },
    "traceparent": {
      "type": "string",
      "pattern": "^00-[0-9a-f]{32}-[0-9a-f]{16}-[0-9a-f]{2}$",
      "description": "W3C traceparent header for distributed tracing"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Event publication timestamp in UTC"
    }
  },
  "additionalProperties": false
}
