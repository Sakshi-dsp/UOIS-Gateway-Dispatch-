{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dispatch.arch/schemas/events/consumed/location/soft_arrived.json",
  "title": "Soft Arrived Event",
  "description": "Event published by Location Service when rider is within 100m of pickup or drop location. Order Service consumes this event for automatic state transitions. Generated from Location Service internal/models/events.go SoftArrivedEvent struct.",
  "type": "object",
  "required": [
    "event_type",
    "dispatch_order_id",
    "location_type",
    "timestamp"
  ],
  "properties": {
    "event_type": {
      "type": "string",
      "const": "soft_arrived",
      "description": "Event type discriminator"
    },
    "dispatch_order_id": {
      "type": "string",
      "description": "Order identifier in human-readable format"
    },
    "location_type": {
      "type": "string",
      "enum": ["ORIGIN", "DESTINATION"],
      "description": "Whether soft arrival is at pickup (ORIGIN) or drop (DESTINATION) location"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Event timestamp in UTC"
    }
  },
  "additionalProperties": false
}
